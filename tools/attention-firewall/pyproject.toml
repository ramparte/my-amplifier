[project]
name = "tool-attention-firewall"
version = "1.0.0"
description = "Amplifier tool for querying Attention Firewall database"
requires-python = ">=3.10"
dependencies = []

[project.entry-points."amplifier.tools"]
attention_firewall = "tool_attention_firewall:AttentionFirewallTool"
